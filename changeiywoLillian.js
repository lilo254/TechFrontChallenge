class Darasa {
  constructor(name=['Jeff', 'Linda', 'Grace', 'Mike', 'Matt', 'Juliet'])
}

defaddClassMember(name) {
  if (var i=0; i<name.length; i++) {
    return "Darasa member added";
  } else {
    return "Member exists";
  }
}

console.log(addClassMember(name));


def removeClassMember(name) {
  if (var i=0; i>name.length; i++) {
    return "Darasa member removed from class";
  } else {
    return "Member does not exists";
  }
}

console.log(removeClassMember(name));

def getDarasaMembers(name)
console.log(getDarasaMembers(name));


class Darasa {
  constructor(name = shule ['standard1', 'standard2', 'standard3']) {
    
    this.name = name

  }
  def addDarasa(name,darasa)
  toString(){
    return this.name
  }
  def removeDarasa(name)
  toString(){
    return this.name
  }

  def getshule()
    toString(){
      return this.name
    }

}